It seems that we would receive the follwing return values for the code given:


let trees = ['birch', 'pine', 'sequoia', 'palm tree'];

trees[trees.length - 1]; // 'palm tree'
trees.pop();              // 'palm tree'
trees[trees.length - 1]; // 'sequoia'