// What will the following code log to the console and why? Don't run it until you have tried to answer.

let a = 1;

function myFunction() {
  console.log(a);
}

myFunction();

// This should log 1 as if there is no matching variable in the current scope the earlier scopes will then be checked.