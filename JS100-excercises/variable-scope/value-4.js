// What will the following code log to the console and why? Don't run it until you have tried to answer.


function myFunction() {
  let a = 1;

  if (true) {
    console.log(a);
  }
}

myFunction();


// This should log 1 as even though the a referenced is in the block of an if statement, that is enclosed within the block let a was executed